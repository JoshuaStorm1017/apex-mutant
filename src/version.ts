/** Kept in sync with package.json by a test (see test/version.test.ts) so exported
 * artifacts can name the exact tool version that produced them without reading
 * package.json at runtime from an unknown install layout. */
export const TOOL_NAME = 'apex-mutant';
export const VERSION = '0.1.0-alpha.2';
